# 本機用 `vercel dev` 測試時使用；正式環境請到 Vercel 專案後台
# Settings -> Environment Variables 設定同樣的值（不要提交 .env 到 git）。

# Supabase 專案設定 -> API 頁面可以找到這三個值
SUPABASE_URL=https://xxxxxxxx.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Supabase 專案設定 -> API -> JWT Settings -> JWT Secret（舊版共用密鑰，用來簽發自訂登入token）
SUPABASE_JWT_SECRET=your-jwt-secret
